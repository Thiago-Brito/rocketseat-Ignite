import { styled } from 'styled-components'

export const OrderCoffeeContainer = styled.div`
  border-radius: 6px 44px;
  background: ${(props) => props.theme['base-card']};
  width: 45rem;
  padding: 4rem;
`
